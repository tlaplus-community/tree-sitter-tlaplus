=============|||
RECURSIVE inside LET/IN
=============|||

---- MODULE Test ----
op ==
  LET
    RECURSIVE f(_)
    f(n) == IF n =< 0 THEN 1 ELSE n * f(n-1)
  IN f(5)
====

-------------|||

(source_file (module (single_line) (identifier) (single_line)
  (unit (operator_definition (identifier) (def_eq)
    (let_in
      (recursive_operator_declaration (operator_declaration (identifier) (placeholder)))
      (operator_definition (identifier) (identifier) (def_eq)
        (if_then_else
          (bound_infix_op (identifier) (leq) (number (nat_number)))
          (number (nat_number))
          (bound_infix_op
            (identifier)
            (mul)
            (bound_op
              (identifer)
              (bound_infix_op
                (identifier)
                (minus)
                (number (nat_number))
              )
            )
          )
        )
      )
      (bound_op (identifer) (number (nat_number)))
    )
  ))
(double_line)))

=============|||
Conjlist with LET/IN
=============|||

---- MODULE Test ----
op ==
  /\ LET x == 1 IN 2
  /\ 3
====

-------------|||

(source_file (module (single_line) (identifier) (single_line)
  (unit (operator_definition
    (identifier) (def_eq)
    (conj_list
      (conj_item
        (bullet_conj)
        (let_in
          (operator_definition (identifier) (def_eq) (number (nat_number)))
          (number (nat_number))
        )
      )
      (conj_item (bullet_conj) (number (nat_number)))
    )
  ))
(double_line)))

=============|||
Conjlist inside LET/IN
=============|||

---- MODULE Test ----
op ==
  LET
    x ==
      /\ 1
      /\ 2
        IN 3
====

-------------|||

(source_file (module (single_line) (identifier) (single_line)
  (unit (operator_definition
    (identifier) (def_eq)
    (let_in
      (operator_definition (identifier) (def_eq)
        (conj_list
          (conj_item (bullet_conj) (number (nat_number)))
          (conj_item (bullet_conj) (number (nat_number)))
        )
      )
      (number (nat_number))
    )
  ))
(double_line)))

=============|||
Disjlist with LET/IN
=============|||

---- MODULE Test ----
op ==
  \/ LET x == 1 IN 2
  \/ 3
====

-------------|||

(source_file (module (single_line) (identifier) (single_line)
  (unit (operator_definition
    (identifier) (def_eq)
    (disj_list
      (disj_item
        (bullet_disj)
        (let_in
          (operator_definition (identifier) (def_eq) (number (nat_number)))
          (number (nat_number))
        )
      )
      (disj_item (bullet_disj) (number (nat_number)))
    )
  ))
(double_line)))

=============|||
Disjlist inside LET/IN
=============|||

---- MODULE Test ----
op ==
  LET
    x ==
      \/ 1
      \/ 2
        IN 3
====

-------------|||

(source_file (module (single_line) (identifier) (single_line)
  (unit (operator_definition
    (identifier) (def_eq)
    (let_in
      (operator_definition (identifier) (def_eq)
        (disj_list
          (disj_item (bullet_disj) (number (nat_number)))
          (disj_item (bullet_disj) (number (nat_number)))
        )
      )
      (number (nat_number))
    )
  ))
(double_line)))
